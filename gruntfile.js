module.exports = function(grunt) {

  // Project configuration.
  grunt.initConfig({
  
  });
  // Load the plugins tasks 
  grunt.loadNpmTasks('default');

	
  // Default task(s).
  grunt.registerTask('default', [ ]);
};